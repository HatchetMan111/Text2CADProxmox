fastapi==0.115.*
uvicorn[standard]==0.30.*
httpx==0.27.*
pydantic==2.*
python-multipart==0.0.9
# CAD-Stack aus earthtojake/text-to-cad (Skill: cad)
cadgen[snapshot]==0.5.1
build123d==0.10.*
# DfAM / Mesh-Checks (3D-Druck-Optimierung)
trimesh==4.*
numpy>=2,<3  # build123d 0.10 braucht numpy>=2 (kein 1.x-Pin!)
# Snapshot-Fallback braucht Playwright nur optional:
# python -m playwright install chromium  (wird im Installer versucht, kein Hard-Fail)
